# Docker 실습 진행 상황

## 현재 단계: Step 1-2 (frontend/nginx.conf 작성)

## 체크리스트

### 사전 준비
- [x] Docker Desktop 설치 및 실행 (Docker 29.2.1)
- [x] VS Code Docker 익스텐션 설치
- [x] `docker --version` 확인
- [x] `docker compose version` 확인 (Compose v5.0.2)

### Step 1: Dockerfile 작성
- [x] `frontend/Dockerfile` 생성 (주석 포함 완료)
- [ ] `frontend/nginx.conf` 생성
- [ ] `backend/Dockerfile` 생성
- [ ] `ai-server/Dockerfile` 생성

### Step 2: Docker Compose 작성
- [ ] `docker-compose.yml` 생성

### Step 3: .dockerignore 생성
- [ ] `frontend/.dockerignore` 생성
- [ ] `backend/.dockerignore` 생성
- [ ] `ai-server/.dockerignore` 생성

### Step 4: 빌드 & 실행
- [ ] `docker compose up --build` 성공
- [ ] 모든 컨테이너 정상 실행 확인

### Step 5: 동작 확인
- [ ] 프론트엔드 (http://localhost) 접속 확인
- [ ] 백엔드 API (http://localhost:8080) 응답 확인
- [ ] AI 서버 (http://localhost:5001) 응답 확인

## 메모
-
