# 대형폐기물 배출 도우미 서비스 Design Document

> **Summary**: 대형폐기물 수수료 조회, 오프라인/온라인 배출, 운반 대행, 역경매를 제공하는 모바일 우선 웹 서비스의 상세 설계
>
> **Project**: throw_it
> **Version**: 0.1.0
> **Author**: User
> **Date**: 2026-02-12
> **Status**: Draft
> **Planning Doc**: [bulk-waste-disposal.plan.md](../../01-plan/features/bulk-waste-disposal.plan.md)

---

## 1. Overview

### 1.1 Design Goals

- 모바일 UI 기준으로 모든 화면 설계 (375px 기준, 반응형 대응)
- 추후 Spring 백엔드, DB, 지도 API, 결제 API 연동이 용이한 확장 가능한 구조
- 프로토타입 단계에서는 Mock 데이터로 동작하되, API 레이어 분리
- 지도 UI 자리를 미리 확보하여 추후 API 연동만으로 동작 가능하도록 설계

### 1.2 Design Principles

- **모바일 우선**: 모든 컴포넌트를 모바일 뷰포트 기준으로 설계
- **Feature-based 모듈화**: 기능별 독립 모듈로 유지보수 용이
- **Data Layer 분리**: Mock 데이터와 실제 API를 교체할 수 있는 서비스 레이어
- **점진적 확장**: 프로토타입 → DB 연동 → 결제/인증/지도 연동 순서로 확장

---

## 2. Architecture

### 2.1 Component Diagram

```
┌──────────────────────────────────────────────────────────┐
│                Client (React + Vite)                      │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Pages   │  │Components│  │ Features │  │  Stores  │ │
│  │(React   │──│(UI/Waste/│──│(fee/     │──│(Zustand) │ │
│  │ Router) │  │ Layout/  │  │disposal/ │  │          │ │
│  │         │  │ Map)     │  │recycle/  │  │          │ │
│  │         │  │          │  │mypage)   │  │          │ │
│  └─────────┘  └──────────┘  └──────────┘  └──────────┘ │
│       │                          │                       │
│  ┌────▼──────────────────────────▼──────────────────┐   │
│  │              Services Layer                       │   │
│  │  (Mock Data ↔ 추후 Spring API Client 교체)        │   │
│  └───────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
           │ (추후 연동)
┌──────────▼──────────────────────────────────────────────┐
│              Backend (Java + Spring Boot)                │
│  REST API → Service → Repository → Database             │
└─────────────────────────────────────────────────────────┘
```

### 2.2 Data Flow

```
사용자 입력 → Page(React Router) → Feature Hook → Service Layer → Mock Data/API
                                      ↓
                                 Zustand Store → UI 업데이트
```

### 2.3 Dependencies

| Component | Depends On | Purpose |
|-----------|-----------|---------|
| Pages | Components, Features | 화면 렌더링 |
| Features | Services, Types, Stores | 비즈니스 로직 |
| Services | Types, Mock Data | 데이터 접근 |
| Components | Types | UI 표현 |
| Stores | Types | 전역 상태 관리 |

---

## 3. Data Model

### 3.1 Entity Definition

```typescript
// 지역 정보
interface Region {
  id: string;
  city: string;          // 시/도 (예: "서울특별시")
  district: string;      // 구 (예: "강남구")
  dong: string;          // 동 (예: "역삼동")
}

// 폐기물 카테고리 (트리 구조)
interface WasteCategory {
  id: string;
  name: string;          // 카테고리명 (예: "가구")
  parentId: string | null; // 상위 카테고리 ID (null이면 최상위)
  children?: WasteCategory[];
}

// 폐기물 항목
interface WasteItem {
  id: string;
  categoryId: string;    // 소속 카테고리 ID
  name: string;          // 폐기물명 (예: "책상")
  sizes: WasteSize[];    // 규격 목록
}

// 폐기물 규격
interface WasteSize {
  id: string;
  label: string;         // 규격 표시 (예: "소형 (1m 이하)")
  description: string;   // 상세 설명
}

// 수수료 정보
interface FeeInfo {
  id: string;
  regionId: string;      // 지역 ID
  wasteItemId: string;   // 폐기물 항목 ID
  sizeId: string;        // 규격 ID
  fee: number;           // 수수료 (원)
}

// 배출 신청
interface DisposalApplication {
  id: string;
  applicationNumber: string;  // 배출 번호
  userId: string;
  regionId: string;
  items: DisposalItem[];
  disposalAddress: string;    // 배출 장소
  preferredDate: string;      // 희망 배출일 (YYYY-MM-DD)
  totalFee: number;
  status: DisposalStatus;
  paymentMethod: PaymentMethod | null;
  createdAt: string;
  updatedAt: string;
}

type DisposalStatus = 'draft' | 'pending_payment' | 'paid' | 'scheduled' | 'collected' | 'cancelled' | 'refunded';
type PaymentMethod = 'card' | 'transfer';

// 배출 품목 (신청 내 개별 항목)
interface DisposalItem {
  wasteItemId: string;
  wasteItemName: string;
  sizeId: string;
  sizeLabel: string;
  quantity: number;
  fee: number;
  photoUrl?: string;     // 사진 (선택)
}

// 운반 대행 업체
interface TransportCompany {
  id: string;
  name: string;          // 업체명
  phone: string;         // 전화번호
  regionId: string;      // 서비스 지역
  description?: string;
}

// 스티커 판매소
interface StickerShop {
  id: string;
  name: string;          // 판매소명 (예: "역삼동 CU 편의점")
  address: string;       // 주소
  phone?: string;
  regionId: string;
  lat?: number;          // 위도 (추후 지도 연동)
  lng?: number;          // 경도 (추후 지도 연동)
}

// 주민센터/동사무소
interface CommunityCenter {
  id: string;
  name: string;
  address: string;
  phone: string;
  regionId: string;
  lat?: number;
  lng?: number;
}

// 역경매 등록 물품
interface RecycleItem {
  id: string;
  userId: string;
  title: string;         // 물품명
  description: string;   // 설명
  photos: string[];      // 사진 URL 목록
  categoryId: string;
  regionId: string;
  address: string;       // 수거 가능 위치
  lat?: number;
  lng?: number;
  status: RecycleStatus;
  createdAt: string;
}

type RecycleStatus = 'available' | 'reserved' | 'collected';
```

### 3.2 Entity Relationships

```
[Region] 1 ──── N [FeeInfo]
   │
   ├── 1 ──── N [StickerShop]
   ├── 1 ──── N [CommunityCenter]
   └── 1 ──── N [TransportCompany]

[WasteCategory] 1 ──── N [WasteCategory] (self-referencing tree)
   │
   └── 1 ──── N [WasteItem]
                    │
                    └── 1 ──── N [WasteSize]

[DisposalApplication] 1 ──── N [DisposalItem]

[RecycleItem] (독립)
```

---

## 4. API Specification (Services Layer)

프로토타입 단계에서는 Mock 데이터를 반환하는 서비스 함수로 구현. 추후 Spring API Client로 교체.

### 4.1 서비스 인터페이스

| Service | Method | Description | Returns |
|---------|--------|-------------|---------|
| **RegionService** | | | |
| | `getRegions()` | 전체 지역 목록 | `Region[]` |
| | `searchRegion(query)` | 주소 검색 → 지역 매핑 | `Region` |
| **WasteService** | | | |
| | `getCategories()` | 전체 카테고리 트리 | `WasteCategory[]` |
| | `getItemsByCategory(categoryId)` | 카테고리별 폐기물 목록 | `WasteItem[]` |
| | `searchWasteItems(keyword)` | 키워드 검색 | `WasteItem[]` |
| **FeeService** | | | |
| | `calculateFee(regionId, wasteItemId, sizeId)` | 수수료 조회 | `FeeInfo` |
| | `calculateTotalFee(items)` | 복수 항목 총 수수료 | `number` |
| **DisposalService** | | | |
| | `createApplication(data)` | 배출 신청 생성 | `DisposalApplication` |
| | `getApplication(id)` | 신청 상세 조회 | `DisposalApplication` |
| | `getMyApplications()` | 내 신청 목록 | `DisposalApplication[]` |
| | `cancelApplication(id)` | 신청 취소 | `DisposalApplication` |
| | `processPayment(id, method)` | 결제 처리 (Mock) | `DisposalApplication` |
| **OfflineService** | | | |
| | `getStickerShops(regionId)` | 스티커 판매소 목록 | `StickerShop[]` |
| | `getCommunityCenters(regionId)` | 주민센터 목록 | `CommunityCenter[]` |
| | `getTransportCompanies(regionId)` | 운반 대행 업체 | `TransportCompany[]` |
| **RecycleService** | | | |
| | `registerItem(data)` | 역경매 물품 등록 | `RecycleItem` |
| | `getItems(regionId?)` | 물품 목록 조회 | `RecycleItem[]` |
| | `updateStatus(id, status)` | 상태 변경 | `RecycleItem` |

### 4.2 추후 Spring API 매핑

| Service Method | Spring API Endpoint |
|---------------|---------------------|
| `getRegions()` | `GET /api/regions` |
| `getCategories()` | `GET /api/waste/categories` |
| `searchWasteItems(keyword)` | `GET /api/waste/items?q={keyword}` |
| `calculateFee(...)` | `GET /api/fees?region={}&item={}&size={}` |
| `createApplication(data)` | `POST /api/disposals` |
| `getMyApplications()` | `GET /api/disposals/my` |
| `cancelApplication(id)` | `PATCH /api/disposals/{id}/cancel` |
| `processPayment(id, method)` | `POST /api/disposals/{id}/payment` |
| `getStickerShops(regionId)` | `GET /api/offline/sticker-shops?region={}` |
| `getTransportCompanies(regionId)` | `GET /api/offline/transport?region={}` |
| `registerItem(data)` | `POST /api/recycle/items` |
| `getItems(regionId?)` | `GET /api/recycle/items?region={}` |

---

## 5. UI/UX Design

### 5.1 화면 목록

| # | 화면 | Route | 설명 |
|---|------|-------|------|
| 1 | 홈 | `/` | 메인 진입점, 주요 기능 바로가기 |
| 2 | 수수료 조회 | `/fee-check` | 지역 + 폐기물 선택 → 수수료 표시 |
| 3 | 오프라인 안내 | `/offline` | 오프라인 배출 안내 메뉴 |
| 4 | 스티커 판매소 | `/offline/sticker-shops` | 판매소 목록 + 지도 UI 자리 |
| 5 | 동사무소/주민센터 | `/offline/centers` | 주민센터 목록 + 지도 UI 자리 |
| 6 | 운반 대행 | `/offline/transport` | 운반 업체 목록 |
| 7 | 온라인 배출 | `/online` | 온라인 배출 안내/시작 |
| 8 | 배출 신청 폼 | `/online/apply` | 지역, 폐기물, 규격, 수량, 장소, 날짜 입력 |
| 9 | 검수 화면 | `/online/review` | 입력 정보 최종 확인 |
| 10 | 결제 화면 | `/online/payment` | 결제 방법 선택 + 결제 (Mock) |
| 11 | 완료 화면 | `/online/complete` | 배출번호 발급 + 안내 문구 |
| 12 | 역경매 | `/recycle` | 등록된 물품 목록 |
| 13 | 역경매 등록 | `/recycle/register` | 사진 업로드 + 물품 정보 등록 |
| 14 | 마이페이지 | `/mypage` | 신청 내역, 상태, 취소/환불 |
| 15 | 영수증 조회 | `/mypage/receipt/:id` | 전자 영수증 상세 |

### 5.2 화면 상세 설계

#### 5.2.1 홈 화면 (`/`)

```
┌─────────────────────────────┐
│  🗑️ 대형폐기물 배출 도우미    │  ← Header (서비스 로고/이름)
├─────────────────────────────┤
│                             │
│  ┌─────────────────────────┐│
│  │  💰 수수료 조회하기       ││  ← CTA 버튼 (가장 큰 영역)
│  │  내 폐기물의 수수료를     ││
│  │  바로 확인하세요          ││
│  └─────────────────────────┘│
│                             │
│  ┌────────────┐┌───────────┐│
│  │ 📋 오프라인 ││ 💻 온라인  ││  ← 2x2 그리드
│  │ 배출 안내   ││ 배출 신청  ││
│  └────────────┘└───────────┘│
│  ┌────────────┐┌───────────┐│
│  │ 🚛 운반    ││ ♻️ 재활용  ││
│  │ 대행       ││ 역경매    ││
│  └────────────┘└───────────┘│
│                             │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │  ← BottomNav (고정)
└─────────────────────────────┘
```

#### 5.2.2 수수료 조회 (`/fee-check`)

```
┌─────────────────────────────┐
│  ← 수수료 조회               │  ← Header + 뒤로가기
├─────────────────────────────┤
│                             │
│  Step 1. 지역 선택           │
│  ┌─────────────────────────┐│
│  │ 주소를 입력하세요         ││  ← 텍스트 입력 (자동완성)
│  └─────────────────────────┘│
│  서울특별시 강남구 역삼동      │  ← 파싱 결과 표시
│                             │
│  Step 2. 폐기물 선택         │
│  ┌─────────────────────────┐│
│  │ 🔍 폐기물 검색...        ││  ← 검색바
│  └─────────────────────────┘│
│  ┌─────────────────────────┐│
│  │ ▶ 가구                   ││  ← 카테고리 트리
│  │   ▶ 책상                 ││
│  │     • 일반 책상           ││
│  │     • 컴퓨터 책상         ││
│  │   ▶ 의자                 ││
│  │ ▶ 가전제품               ││
│  │ ▶ 침구류                 ││
│  └─────────────────────────┘│
│                             │
│  Step 3. 규격 선택           │
│  ○ 소형 (1m 이하)           │
│  ● 중형 (1m ~ 1.5m)        │  ← 라디오 선택
│  ○ 대형 (1.5m 초과)         │
│                             │
│  ┌─────────────────────────┐│
│  │  💰 수수료: 8,000원      ││  ← 결과 카드
│  │  강남구 기준 | 책상 중형   ││
│  └─────────────────────────┘│
│                             │
│  [온라인으로 바로 신청하기 →]  │  ← 연계 버튼
│                             │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │
└─────────────────────────────┘
```

#### 5.2.3 온라인 배출 신청 (`/online/apply`)

```
┌─────────────────────────────┐
│  ← 온라인 배출 신청          │
├─────────────────────────────┤
│  진행 상태: ●──○──○──○      │  ← ProgressBar (입력/검수/결제/완료)
│            입력              │
│                             │
│  배출 지역                   │
│  ┌─────────────────────────┐│
│  │ 서울특별시 강남구 역삼동   ││
│  └─────────────────────────┘│
│                             │
│  배출 장소 (상세 주소)        │
│  ┌─────────────────────────┐│
│  │ 역삼동 123-45 아파트 앞   ││
│  └─────────────────────────┘│
│                             │
│  희망 배출일                  │
│  ┌─────────────────────────┐│
│  │ 2026-02-15              ││  ← DatePicker
│  └─────────────────────────┘│
│                             │
│  배출 품목                   │
│  ┌─────────────────────────┐│
│  │ 책상 (중형) x1  8,000원  ││  ← 추가된 품목
│  │                    [삭제]││
│  │ 의자 (소형) x2  6,000원  ││
│  │                    [삭제]││
│  └─────────────────────────┘│
│  [+ 품목 추가]               │
│                             │
│  사진 첨부 (선택)             │
│  [📷 사진 촬영/선택]         │
│                             │
│  총 수수료: 14,000원         │
│                             │
│  [다음: 검수하기 →]          │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │
└─────────────────────────────┘
```

#### 5.2.4 검수 화면 (`/online/review`)

```
┌─────────────────────────────┐
│  ← 신청 내용 확인            │
├─────────────────────────────┤
│  진행 상태: ✓──●──○──○      │
│              검수             │
│                             │
│  📍 배출 정보                │
│  지역: 서울특별시 강남구       │
│  주소: 역삼동 123-45 앞      │
│  희망일: 2026-02-15          │
│                             │
│  🗑️ 배출 품목                │
│  ┌─────────────────────────┐│
│  │ 1. 책상 (중형) x1 8,000 ││
│  │ 2. 의자 (소형) x2 6,000 ││
│  ├─────────────────────────┤│
│  │ 합계:        14,000원    ││
│  └─────────────────────────┘│
│                             │
│  📷 첨부 사진                │
│  [사진 썸네일]               │
│                             │
│  ⚠️ 위 내용이 정확한지       │
│  다시 한번 확인해주세요.      │
│                             │
│  [← 수정하기] [결제하기 →]   │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │
└─────────────────────────────┘
```

#### 5.2.5 결제 완료 / 배출번호 (`/online/complete`)

```
┌─────────────────────────────┐
│  배출 신청 완료 ✅            │
├─────────────────────────────┤
│  진행 상태: ✓──✓──✓──●      │
│                   완료       │
│                             │
│  ┌─────────────────────────┐│
│  │                         ││
│  │   배출 번호              ││
│  │   ┌─────────────────┐   ││
│  │   │  GN-2026-0215   │   ││  ← 크게 표시
│  │   │  -00123         │   ││
│  │   └─────────────────┘   ││
│  │                         ││
│  │ ✏️ 이 번호를 종이에     ││
│  │ 크게 적어서 폐기물에    ││
│  │ 붙여주세요!             ││
│  │                         ││
│  └─────────────────────────┘│
│                             │
│  배출일: 2026-02-15         │
│  배출장소: 역삼동 123-45 앞  │
│  결제금액: 14,000원          │
│                             │
│  [📄 영수증 보기]            │
│  [🏠 홈으로 돌아가기]        │
│                             │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │
└─────────────────────────────┘
```

#### 5.2.6 스티커 판매소 (`/offline/sticker-shops`)

```
┌─────────────────────────────┐
│  ← 스티커 판매소             │
├─────────────────────────────┤
│  지역 선택                   │
│  ┌─────────────────────────┐│
│  │ 서울특별시 강남구         ││
│  └─────────────────────────┘│
│                             │
│  ┌─────────────────────────┐│
│  │                         ││
│  │    🗺️ 지도 영역          ││  ← MapPlaceholder
│  │    (추후 API 연동 시     ││     컴포넌트
│  │     지도가 표시됩니다)    ││
│  │                         ││
│  └─────────────────────────┘│
│                             │
│  판매소 목록 (3건)           │
│  ┌─────────────────────────┐│
│  │ 📍 역삼동 CU 편의점      ││
│  │ 강남구 역삼로 123        ││
│  │ 📞 02-1234-5678         ││
│  ├─────────────────────────┤│
│  │ 📍 삼성동 GS25           ││
│  │ 강남구 삼성로 456        ││
│  │ 📞 02-2345-6789         ││
│  └─────────────────────────┘│
│                             │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │
└─────────────────────────────┘
```

#### 5.2.7 마이페이지 (`/mypage`)

```
┌─────────────────────────────┐
│  마이페이지                  │
├─────────────────────────────┤
│                             │
│  신청 내역                   │
│  ┌─────────────────────────┐│
│  │ 📋 GN-2026-0215-00123  ││
│  │ 책상(중형) x1, 의자 x2  ││
│  │ 상태: 결제완료 🟢        ││
│  │ 신청일: 2026-02-12       ││
│  │ [상세보기] [취소하기]     ││
│  ├─────────────────────────┤│
│  │ 📋 GN-2026-0210-00098  ││
│  │ 소파(대형) x1            ││
│  │ 상태: 수거완료 ✅         ││
│  │ 신청일: 2026-02-10       ││
│  │ [상세보기] [영수증]       ││
│  ├─────────────────────────┤│
│  │ 📋 GN-2026-0208-00075  ││
│  │ 냉장고(대형) x1          ││
│  │ 상태: 취소됨 ❌           ││
│  │ 신청일: 2026-02-08       ││
│  │ [상세보기]               ││
│  └─────────────────────────┘│
│                             │
├─────────────────────────────┤
│  🏠 홈    📋 내역    👤 MY  │
└─────────────────────────────┘
```

### 5.3 Component List

| Component | Location | Responsibility |
|-----------|----------|----------------|
| **Layout** | | |
| `Header` | `components/layout/` | 상단 헤더 (제목, 뒤로가기) |
| `BottomNav` | `components/layout/` | 하단 네비게이션 (홈, 내역, MY) |
| `MobileContainer` | `components/layout/` | 모바일 최대 너비 래퍼 |
| `ProgressBar` | `components/layout/` | 단계별 진행 표시 |
| **UI 공통** | | |
| `Button` | `components/ui/` | 공통 버튼 |
| `Input` | `components/ui/` | 텍스트 입력 |
| `Card` | `components/ui/` | 카드 컨테이너 |
| `Select` | `components/ui/` | 드롭다운 선택 |
| `DatePicker` | `components/ui/` | 날짜 선택 |
| `Modal` | `components/ui/` | 모달/바텀시트 |
| `Badge` | `components/ui/` | 상태 뱃지 |
| `SearchBar` | `components/ui/` | 검색 입력 |
| **폐기물 관련** | | |
| `CategoryTree` | `components/waste/` | 트리 구조 카테고리 선택 |
| `WasteSearchBar` | `components/waste/` | 폐기물 키워드 검색 |
| `SizeSelector` | `components/waste/` | 규격 라디오 선택 |
| `FeeResultCard` | `components/waste/` | 수수료 결과 표시 카드 |
| `WasteItemCard` | `components/waste/` | 배출 품목 항목 카드 |
| **지도 관련** | | |
| `MapPlaceholder` | `components/map/` | 지도 자리 표시 (추후 API 연동) |
| `LocationCard` | `components/map/` | 장소 정보 카드 (이름, 주소, 전화번호) |
| **배출 관련** | | |
| `DisposalForm` | `features/disposal/` | 배출 신청 폼 |
| `ReviewSummary` | `features/disposal/` | 검수 요약 |
| `PaymentForm` | `features/disposal/` | 결제 UI (Mock) |
| `DisposalNumber` | `features/disposal/` | 배출번호 표시 |
| `ReceiptView` | `features/disposal/` | 전자 영수증 |
| **마이페이지 관련** | | |
| `ApplicationList` | `features/mypage/` | 신청 내역 목록 |
| `ApplicationCard` | `features/mypage/` | 개별 신청 카드 |
| `StatusBadge` | `features/mypage/` | 상태 뱃지 (결제완료/수거완료/취소) |
| **역경매 관련** | | |
| `RecycleItemCard` | `features/recycle/` | 역경매 물품 카드 |
| `PhotoUploader` | `features/recycle/` | 사진 업로드 |
| `RecycleRegisterForm` | `features/recycle/` | 역경매 등록 폼 |

---

## 6. Error Handling

### 6.1 Error Code Definition

| Code | Message | Cause | Handling |
|------|---------|-------|----------|
| `REGION_NOT_FOUND` | 해당 지역을 찾을 수 없습니다 | 잘못된 주소 입력 | 재입력 안내 |
| `FEE_NOT_AVAILABLE` | 수수료 정보가 없습니다 | 해당 지역/품목 미지원 | 구청 문의 안내 |
| `INVALID_DATE` | 배출 가능한 날짜를 선택해주세요 | 과거 날짜 선택 | DatePicker 제한 |
| `PAYMENT_FAILED` | 결제에 실패했습니다 | 결제 오류 | 재시도 안내 |
| `CANCEL_NOT_ALLOWED` | 취소할 수 없는 상태입니다 | 이미 수거 완료 | 상태 안내 |
| `FILE_TOO_LARGE` | 사진 크기가 너무 큽니다 | 파일 크기 초과 | 5MB 이하 안내 |

### 6.2 Error Response Format

```typescript
interface AppError {
  code: string;
  message: string;        // 사용자 친화적 메시지 (한국어)
  details?: unknown;
}
```

---

## 7. Security Considerations

- [x] Input validation: 모든 사용자 입력 검증 (XSS 방지)
- [ ] Authentication: 추후 본인 인증 연동 시 구현
- [ ] HTTPS enforcement: 배포 시 적용
- [x] 파일 업로드 제한: 5MB, 이미지 파일만 허용
- [x] 결제 정보 클라이언트 미저장 (프로토타입이므로 Mock)

---

## 8. Test Plan

### 8.1 Test Scope

| Type | Target | Tool |
|------|--------|------|
| 수동 테스트 | 주요 User Flow | 브라우저 (모바일 뷰) |
| Lint | 코드 품질 | ESLint |
| Build | 빌드 성공 여부 | `next build` |

### 8.2 Test Cases (Key)

- [ ] 수수료 조회: 지역 선택 → 카테고리 선택 → 규격 선택 → 수수료 정상 표시
- [ ] 온라인 배출: 폼 입력 → 검수 확인 → 결제 → 배출번호 발급
- [ ] 마이페이지: 신청 내역 표시, 취소 동작
- [ ] 카테고리 트리: 펼치기/접기, 검색 동작
- [ ] 지도 자리: MapPlaceholder가 정상 렌더링
- [ ] 모바일 반응형: 375px~428px 뷰포트에서 레이아웃 정상

---

## 9. Clean Architecture

### 9.1 Layer Structure

| Layer | Responsibility | Location |
|-------|---------------|----------|
| **Presentation** | UI 컴포넌트, 페이지, 레이아웃 | `src/pages/`, `src/components/` |
| **Application** | Feature hooks, 비즈니스 로직 | `src/features/*/`, `src/stores/` |
| **Domain** | 타입 정의, 비즈니스 규칙 | `src/types/` |
| **Infrastructure** | 서비스 레이어, API 클라이언트, Mock 데이터 | `src/services/`, `src/lib/mock-data/` |

### 9.2 This Feature's Layer Assignment

| Component | Layer | Location |
|-----------|-------|----------|
| Pages (React Router) | Presentation | `src/pages/*.tsx` |
| UI Components | Presentation | `src/components/ui/` |
| CategoryTree, FeeResultCard | Presentation | `src/components/waste/` |
| MapPlaceholder | Presentation | `src/components/map/` |
| DisposalForm, ReviewSummary | Application | `src/features/disposal/` |
| ApplicationList | Application | `src/features/mypage/` |
| FeeService, WasteService | Infrastructure | `src/services/` |
| Mock Data | Infrastructure | `src/lib/mock-data/` |
| Types (Region, WasteItem 등) | Domain | `src/types/` |
| Zustand Stores | Application | `src/stores/` |

---

## 10. Coding Convention Reference

### 10.1 This Feature's Conventions

| Item | Convention Applied |
|------|-------------------|
| Component naming | PascalCase (예: `CategoryTree.tsx`) |
| File organization | Feature-based (`features/fee/`, `features/disposal/`) |
| State management | Zustand store per feature |
| Error handling | AppError 타입 통일, 한국어 사용자 메시지 |
| API 통신 | `src/services/` 레이어를 통해서만 데이터 접근 |
| Mock 데이터 | `src/lib/mock-data/`에 JSON 형태로 관리 |

---

## 11. Implementation Guide

### 11.1 File Structure

```
src/
├── main.tsx                          # 엔트리 포인트
├── App.tsx                           # App 컴포넌트 (Router + Layout)
├── router/
│   └── index.tsx                     # React Router 라우트 정의
├── pages/
│   ├── HomePage.tsx                  # 홈
│   ├── FeeCheckPage.tsx              # 수수료 조회
│   ├── offline/
│   │   ├── OfflinePage.tsx           # 오프라인 안내
│   │   ├── StickerShopsPage.tsx      # 스티커 판매소
│   │   ├── CentersPage.tsx           # 동사무소/주민센터
│   │   └── TransportPage.tsx         # 운반 대행
│   ├── online/
│   │   ├── OnlinePage.tsx            # 온라인 안내
│   │   ├── ApplyPage.tsx             # 배출 신청 폼
│   │   ├── ReviewPage.tsx            # 검수
│   │   ├── PaymentPage.tsx           # 결제
│   │   └── CompletePage.tsx          # 완료/배출번호
│   ├── recycle/
│   │   ├── RecyclePage.tsx           # 역경매 목록
│   │   └── RegisterPage.tsx          # 역경매 등록
│   └── mypage/
│       ├── MyPage.tsx                # 마이페이지
│       └── ReceiptPage.tsx           # 영수증 상세
├── components/
│   ├── ui/
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Card.tsx
│   │   ├── Select.tsx
│   │   ├── DatePicker.tsx
│   │   ├── Modal.tsx
│   │   ├── Badge.tsx
│   │   └── SearchBar.tsx
│   ├── waste/
│   │   ├── CategoryTree.tsx
│   │   ├── WasteSearchBar.tsx
│   │   ├── SizeSelector.tsx
│   │   ├── FeeResultCard.tsx
│   │   └── WasteItemCard.tsx
│   ├── map/
│   │   ├── MapPlaceholder.tsx
│   │   └── LocationCard.tsx
│   └── layout/
│       ├── Header.tsx
│       ├── BottomNav.tsx
│       ├── MobileContainer.tsx
│       └── ProgressBar.tsx
├── features/
│   ├── fee/
│   │   └── useFeeCheck.ts           # 수수료 조회 훅
│   ├── disposal/
│   │   ├── useDisposalForm.ts        # 배출 신청 폼 훅
│   │   ├── DisposalForm.tsx
│   │   ├── ReviewSummary.tsx
│   │   ├── PaymentForm.tsx
│   │   └── DisposalNumber.tsx
│   ├── recycle/
│   │   ├── useRecycle.ts
│   │   ├── RecycleItemCard.tsx
│   │   ├── PhotoUploader.tsx
│   │   └── RecycleRegisterForm.tsx
│   └── mypage/
│       ├── useMyApplications.ts
│       ├── ApplicationList.tsx
│       ├── ApplicationCard.tsx
│       ├── StatusBadge.tsx
│       └── ReceiptView.tsx
├── services/
│   ├── regionService.ts
│   ├── wasteService.ts
│   ├── feeService.ts
│   ├── disposalService.ts
│   ├── offlineService.ts
│   └── recycleService.ts
├── types/
│   ├── region.ts
│   ├── waste.ts
│   ├── fee.ts
│   ├── disposal.ts
│   ├── offline.ts
│   └── recycle.ts
├── stores/
│   ├── useRegionStore.ts
│   └── useDisposalStore.ts
└── lib/
    └── mock-data/
        ├── regions.json
        ├── waste-categories.json
        ├── waste-items.json
        ├── fees.json
        ├── sticker-shops.json
        ├── community-centers.json
        ├── transport-companies.json
        └── sample-applications.json
```

### 11.2 Implementation Order

| 순서 | 작업 | 의존성 | 산출물 |
|------|------|--------|--------|
| 1 | 프로젝트 초기 설정 | 없음 | React + Vite + TypeScript + Tailwind 프로젝트 |
| 2 | 타입 정의 | 없음 | `src/types/*.ts` |
| 3 | Mock 데이터 작성 | 타입 정의 | `src/lib/mock-data/*.json` |
| 4 | 공통 UI 컴포넌트 | 없음 | `src/components/ui/*` |
| 5 | 레이아웃 컴포넌트 | UI 컴포넌트 | `src/components/layout/*` |
| 6 | 서비스 레이어 | 타입, Mock 데이터 | `src/services/*` |
| 7 | 라우터 + 홈 화면 | 레이아웃 | `src/router/`, `src/pages/HomePage.tsx` |
| 8 | 수수료 조회 (Phase 1) | 서비스, 카테고리트리 | `src/pages/FeeCheckPage.tsx`, `components/waste/` |
| 9 | 온라인 배출 신청 (Phase 2) | 서비스, 폼 | `src/pages/online/*`, `features/disposal/` |
| 10 | 오프라인 안내 + 운반 대행 (Phase 3) | 서비스, 지도placeholder | `src/pages/offline/*`, `components/map/` |
| 11 | 마이페이지 (Phase 4) | 서비스, 스토어 | `src/pages/mypage/*`, `features/mypage/` |
| 12 | 역경매 (Phase 5) | 서비스 | `src/pages/recycle/*`, `features/recycle/` |

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-12 | Initial draft based on Plan document | User |
| 0.2 | 2026-02-12 | Frontend 기술스택 Next.js → React + Vite 변경 | User |
